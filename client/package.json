{
  "name": "ohos-test-client",
  "version": "0.1.0",
  "private": true,
  "description": "RK3568 client agent for device registration and task execution",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "pnpm run build && node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.5.14",
    "typescript": "^5.7.3"
  }
}
